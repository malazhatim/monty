#include "monty.h"
/**
 * main - point
 * @atgc: arguments
 * @argv: array
 * Return: always 0
 */
int maim(int argc, char *argv)
{
	if (argc != 2)
	{
		mon_errors(1);
		exit(EXIT_FAILURE);
	}
	op_file(argv[]);
	node_free();
	return (0);
}
